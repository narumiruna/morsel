# Morsel

> Morsel is a small, self-hosted, API-first service for creating capability-protected Markdown shares and rendering them in a same-origin web viewer.

The public service is available at `https://morsel.narumi.dev/`. It supports GitHub Flavored Markdown, syntax-highlighted fenced code, KaTeX, Mermaid, and fenced `vega-lite` charts with inline data. Authored HTML is not executed, and Vega-Lite charts cannot load external resources.

Important usage notes:

- Creating and revoking shares requires an administrative bearer API key.
- Every successful `GET /v1/shares/{share}` consumes exactly one view. Do not retrieve or validate a share unless the user explicitly asks you to access it.
- An opt-in `GET /s/{capability}` Open Graph excerpt does not consume a view, but it still exposes share content and must not be fetched without the user's request.
- Treat API keys, raw capability tokens, and complete share URLs as secrets. Do not expose or log them.
- Viewer URLs use `#/s/{capability}` by default. Preview-enabled shares use `/s/{capability}` so Telegram can fetch metadata; a browser then sends the capability to the same-origin retrieval API.
- Retrieval responses must not be cached. Expired, revoked, and exhausted shares are unavailable.

## Documentation

- [Morsel documentation](https://raw.githubusercontent.com/narumiruna/morsel/main/README.md): Setup, API examples, retrieval semantics, configuration, operations, and security model.
- [Morsel OpenAPI contract](https://raw.githubusercontent.com/narumiruna/morsel/main/api/openapi.yaml): Complete request, response, authentication, schema, and error definitions for the HTTP API.

## Optional

- [Morsel source repository](https://github.com/narumiruna/morsel): Go API, React viewer, deployment configuration, tests, and issue history.
- [GNU Affero General Public License v3.0](https://raw.githubusercontent.com/narumiruna/morsel/main/LICENSE): AGPL-3.0 terms for using and modifying Morsel.
