{
  "name": "morsel",
  "private": true,
  "workspaces": ["packages/*"],
  "engines": {
    "node": "^24.15.0 || >=26.0.0",
    "npm": ">=11.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspace @morsel/viewer",
    "ci": "npm run ci --workspaces",
    "test:browser": "npm run test:browser --workspace @morsel/viewer"
  },
  "overrides": {
    "lodash-es": "4.18.1"
  }
}
