{
  "name": "@narumitw/morsel",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "^24.15.0 || >=26.0.0",
    "npm": ">=11.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspace @narumitw/morsel-viewer",
    "ci": "biome ci . && npm run ci --workspaces",
    "test": "vitest run",
    "test:browser": "npm run test:browser --workspace @narumitw/morsel-viewer",
    "prepare": "husky"
  },
  "overrides": {
    "lodash-es": "4.18.1"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "vitest": "^5.0.1"
  }
}
